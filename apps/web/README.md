# Web application

React + Vite 기반 관리자 Web입니다. 현재 Phase 0 시작 화면과 API liveness 연결 상태를
제공합니다.

```bash
pnpm --filter @church/web dev
```

개발 서버는 `/api` 요청을 기본적으로 `http://localhost:3000`으로 전달합니다. 실제 개인정보를
fixture, screenshot 또는 test log에 사용하지 않습니다.
